using System;

namespace Variables
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Variables in C#");

            // Declaring Variables
            int i, j, k; // Define more than one int
            char c, ch; // Define more than one char
            double total; // Defining a double

            // Declaring and Initializing Variables
            int n = 0;
            string name = "cmendibl3";
            double average = 23.5;

            // using var
            var z = 0;
            var username = "cmendibl3";
            var pi = 3.14D;
        }
    }
}
